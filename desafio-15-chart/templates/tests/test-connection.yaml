apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "desafio-15-chart.fullname" . }}-test-connection"
  labels:
    {{- include "desafio-15-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "desafio-15-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
